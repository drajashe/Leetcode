"""Reverse a StringThis interview question requires you to reverse a string using recursion. Make sure to think of the base case here.Again, make sure you use recursion to accomplish this. Do not slice (e.g. string[::-1]) or use iteration, there must be a recursive call for the function."""def rev_string_recurssively(s):    if(len(s)<=1):        return s    else:        m = int(len(s)/2)        #print(m)        #print(rev_string_recurssively(s[m:]),"Deeeee")        #print(rev_string_recurssively((s[:m])))        print(rev_string_recurssively(s[m:]) + (rev_string_recurssively((s[:m]))))        return rev_string_recurssively(s[m:]) + (rev_string_recurssively((s[:m])))    passstr="gold"print(rev_string_recurssively(str))